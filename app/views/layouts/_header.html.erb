<header>
    <nav class="navbar navbar-inverse navbar-static-top" role="navigation">
      <div class="container">
          <div class="navbar-header"> <!-- Boton que va a aparecer cuando se haga responsie-->
              <button type ="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navegacion-edu">
                <span class="sr-only"> Desplegar/Ocultar Menu </span>  
                <span class="icon-bar"> </span>  
                <span class="icon-bar"> </span>  
                <span class="icon-bar"> </span>  
              </button>

              <%= link_to 'Educanding',questions_url, class:"navbar-brand"  %> 
            
          </div>
          <div class="collapse navbar-collapse" id="navegacion-edu">
              <ul class="nav navbar-nav">
                
                <li class="dropdown">
                  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">

                    Categorias <span class="caret"> </span>

                  </a>
                  <ul class="dropdown-menu" role="menu">
                    <li><a href="#">Lo que quieras</a></li>
                    <li class="divider"></li>
                    <li><a href="#">Lo que quieras</a></li>
                    <li class="divider"></li>
                    <li><a href="#">Lo que quieras</a></li>
                  </ul>
                </li>
                <% if user_signed_in? %>
                  <li><%= link_to 'Nueva Pregunta', new_question_path %> </li>
                <% end %>
                <li><a href="#">Contacto</a></li>
              </ul>
              <ul class="nav navbar-nav navbar-right registro form-group">
                  
                  <% if user_signed_in? %>
                      <%= current_user.email %>
                      <%= link_to 'Perfil', edit_user_registration_path,class:"btn btn-primary"%> 
                      <%= link_to "Cerrar Sesión", destroy_user_session_path, method: :delete,class:"btn btn-primary"%> 
                  <% else %>

                      <%=link_to "Registrarse",new_user_registration_url,class:"btn btn-primary" %>
                          
                      <%=link_to "Iniciar Sesión",new_user_session_url,class:"btn btn-primary"%>                 
                  <% end %>
                 
              </ul>
              <form action="" class="navbar-form navbar-right" role="search">
                <div class="form-group">
                  <input type="text" class="form-control" placeholder="buscar">
                </div>
                <button type="submit" class="btn btn-primary">
                  <span class="glyphicon glyphicon-search"></span>
                </button>
              </form>
          </div>
      </div>
    </nav>

  </header>
  <section class="jumbotron">
    <div class="container">
      <%= image_tag "logo.png", class:"img-responsive", alt:"Imagen del Logo" %>
      <p>Aplicacion para realizar preguntas y respuestas.</p> 
      
    </div>
  </section>
