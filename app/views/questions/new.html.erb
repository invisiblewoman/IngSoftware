<% if @condicion == "0" %>
   <div class="alert alert-danger" role="alert">No se pudo crear la pregunta, ya que la misma contenia mas de cinco etiquetas</div>
<% end %>
 </div>
<div class="row">
      <section class="posts col-md-9" >
        <div class="miga-de-pan">
          <ol class="breadcrumb">
            <li><%= link_to 'Inicio', questions_path%></li>
            <li>Nueva Pregunta</li>
          </ol>
 </div>
<%= form_for @question do |f| %>
  <div class="form-group">
    <%= f.label :titulo %>
    <%= f.text_field :titulo, class: "form-control", placeholder: "Escriba la pregunta", required:true %>
  </div>
  <div class="form-group">
    <%= f.label :cuerpo, "Cuerpo de la pregunta" %><br>

    <%= f.text_area :cuerpo, :class => "tinymce", placeholder: "Escriba una descripcion de su pregunta" ,rows:15%>
    <%= tinymce %>    
     
    
  </div>
  <div class="checkbox">
    <p>Seleccione su etiqueta (Debe seleccionar como minimo una etiqueta y como maximo cinco)</p> 
    <%= f.collection_select :tag_ids, Tag.all, :id, :nombre, {}, { multiple: true,class:"form-control",required:true  }%>
 
  </div> 
   <div class="checkbox">
    <p>Seleccione su facultad</p> 
    <%= f.collection_select :university_id, University.all, :id, :nombre, {}, { class:"form-control",required:true  }%>
 
  </div> 
    <%= f.submit "Enviar Pregunta", class:"btn btn-default" %>
<% end %>
